#!/usr/bin/env bash
# Find the PID of the 7-highlander process
pid=$(pgrep -f 7-highlander)

# Send signal 9 (SIGKILL) if PID is found
if [ -n "$pid" ]; then
    echo "9" > /proc/"$pid"/sig
else
    echo "Process 7-highlander not running."
fi
